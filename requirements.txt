# Data analysis
matplotlib

# Database
sqlalchemy>=2.0.0
psycopg2-binary
alembic

# Configuration
python-dotenv

# HTTP service and OCR
fastapi
uvicorn
requests
PyPDF2  # PDF page extraction
reportlab  # PDF generation for warm-up
nvidia-ml-py  # GPU monitoring (replaces deprecated pynvml)
# Optional OCR (PaddleOCR-VL)
paddlepaddle-gpu  # or paddlepaddle for CPU-only
paddleocr
